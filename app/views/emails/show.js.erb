$(document).ready(function(){
	$('.emailcontent').hide(); //hiding (reset) all the mail divs
	$('.read a').css({'color' : 'black',}); //set all read tabs at the same background /text color
	$('.read').css({'background-color' : '#F0F0F0',});
	$('#mail_<%=@email.id%>').slideDown(); //display animation

	
	// $('#object_<%=@email.id%> a').css({'color' : 'white', 'font-weight':'normal'}); //set the selected link on not bold and grey for "read status"
	// $('#object_<%=@email.id%>').css({'background-color' : '#0069D9', 'color':'grey'}); // set the selected link tab background color
	// $('#object_<%=@email.id%>').attr('class','read'); // gives the tab the class "read"
	$('#object_<%=@email.id%>').removeClass('ur');
	$('#object_<%=@email.id%>').addClass('read');
});